"""Gradio entry point for AI Level Director Studio.

This is a placeholder scaffold. The UI is built in a later phase and will call the
`LevelDirectorService` facade only, never the prior project internals directly.
Run with: ``python app.py``.
"""

from __future__ import annotations


def main() -> None:
    """Launch the Gradio app. Not yet implemented."""
    raise NotImplementedError("The Gradio UI is implemented in a later phase.")


if __name__ == "__main__":
    main()
