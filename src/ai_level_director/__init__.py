"""AI Level Director Studio.

A designer governed workflow for 2D platformer level iteration. The package
orchestrates three prior capstone components behind adapters: a generative level
source (Project 5), an agentic triage layer (Project 6), and a playtest feedback
classifier (Project 3). Project 7 owns orchestration, candidate lifecycle state,
persistence, reporting, and the UI facing service. It does not build a new model
or agent.
"""
